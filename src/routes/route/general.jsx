import React, { lazy } from 'react';
import { pages } from '../../menu';

const LANDING = {
    // DASHBOARD_COMPANY: lazy(() => import('../../pages/presentation/admin/DashboardCompany')),
};

const presentation = [
    // {
    //     path: pages.admin.companyList.path,
    //     element: <LANDING.DASHBOARD_COMPANY />,
    // },
];

export default presentation;
